{
  "name": "Fe-Take-Home",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-all": "concurrently \"cd backend && yarn start\" \"cd application && yarn start\" \"cd application && yarn ios\"",
    "install-all": "concurrently \"cd backend && yarn install\" \"cd client && yarn install\"",
    "format": "concurrently \"cd backend && npm run format\" \"cd client && npm run format\" \"cd rules-project && npm run format\"",
    "generate": "plop",
    "docker-compose": "docker-compose up -d",
    "plop": "plop"
  },
  "keywords": [],
  "author": "dtanh7",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "devDependencies": {
    "plop": "^2.7.4"
  }
}
