{
  "name": "Fe-Take-Home",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && yarn start\" \"cd client && yarn ios\" \"cd rules-project && yarn start\"",
    "install-all": "concurrently \"cd backend && yarn install\" \"cd client && yarn install\" \"cd rules-project && yarn install\"",
    "format": "concurrently \"cd backend && npm run format\" \"cd client && npm run format\" \"cd rules-project && npm run format\"",
    "generate": "plop",
    "docker-compose": "docker-compose up -d",
    "plop": "plop"
  },
  "keywords": [],
  "author": "dtanh7",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "devDependencies": {
    "plop": "^2.7.4"
  }
}
